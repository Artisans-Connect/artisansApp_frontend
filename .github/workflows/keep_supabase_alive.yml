name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 0 */3 * *' # Runs every 3 days
  workflow_dispatch: # Allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase API
        run: |
            curl -L -X POST 'https://qdeznjpvkhrxesjykovi.supabase.co/functions/v1/health' \
            -H 'Authorization: Bearer sb_publishable_YqekfqmdaSRYENeJMm84sA_jZrQWasf' \
            -H 'Content-Type: application/json' \
            --data '{"name":"Functions"}'
