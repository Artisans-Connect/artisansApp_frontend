name: Maintain OpenWiki documentation

on:
  workflow_dispatch:
  schedule:
    - cron: "23 3 * * 0"

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: openwiki-update
  cancel-in-progress: false

jobs:
  update:
    name: Refresh frontend wiki
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out frontend repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: true
      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Install and launch Ollama
        run: |
          curl -fsSL https://ollama.com/install.sh | sh
          ollama serve &
          sleep 5
          ollama pull qwen2.5-coder:7b
      - name: Update OpenWiki
        run: npm run docs:wiki:update
        env:
          OPENWIKI_PROVIDER: openai-compatible
          OPENAI_COMPATIBLE_BASE_URL: http://localhost:11434/v1
          OPENAI_COMPATIBLE_API_KEY: ollama
          OPENWIKI_MODEL_ID: qwen2.5-coder:7b
          OPENWIKI_TELEMETRY_DISABLED: "1"
      - name: Create or update documentation pull request
        uses: peter-evans/create-pull-request@22a9089034f40e5a961c8808d113e2c98fb63676 # v7
        with:
          add-paths: |
            openwiki/
            AGENTS.md
            CLAUDE.md
            .github/workflows/openwiki-update.yml
          branch: automation/openwiki-update
          delete-branch: true
          commit-message: "docs: update frontend OpenWiki"
          title: "docs: update frontend OpenWiki"
          body: |
            Automated frontend-only documentation refresh.

            The wiki is deliberately scoped to this repository. Cross-repository
            behavior is labeled as an integration boundary rather than documented
            as product-wide fact.
          labels: |
            documentation
            automated
